# --- target surface (Phase 1) ---
flask>=3.0,<4.0

# --- perception & action (Phase 2) ---
playwright>=1.44,<2.0

# --- typed artifact schema (Phase 4) ---
pydantic>=2.7,<3.0

# --- config ---
python-dotenv>=1.0,<2.0

# --- discovery agent (Phase 3): one OpenAI-compatible client covers
#     Gemini / Groq / GitHub Models / Cerebras / OpenRouter / Ollama.
#     See config.py for the provider seam.
openai>=1.30,<2.0

# --- tests ---
pytest>=8.0,<9.0
